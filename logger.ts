// This is for console logs that we want to keep in the codebase
// Please only use this when actually necessary
export const logger = (...values) => {
  // tslint:disable-next-line
  console.log(...values)
}
