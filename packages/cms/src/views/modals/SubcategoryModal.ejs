<!-- Modal -->
<div
  class="modal fade bd-example-modal-lg"
  id="subcategoryModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="dynamicModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="dynamicModalLabel">New message</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <table class="table table-borderless modal-table">
          <thead>
            <tr>
              <th style="width: 10%"><%= __('New Subcategory title')%></th>
              <th style="width: 10%"><%= __('Parent category')%></th>
            </tr>
          </thead>
          <tbody style="overflow-y: scroll">
            <td style="width: 10%">
              <input type="text" class="form-control" id="colSubcategory0TableModal" />
              <p id="errorSubcatUniq1" style="display: none;">title is duplicate</p>
              <p id="errorSubcat1" style="display: none;">max 40 characters</p>
              <p id="countdown3">remaining</p>
            </td>
            <td style="width: 10%">
              <select class="custom-select" id="colSubcategory1TableModal">
                <% categories.forEach((item, index) =>{ %>
                <option value="<%= item.id %>"><%= item.title %></option>
                <% }) %>
              </select>
              <p id="errorSubcat2" style="display: none;">required field</p>
            </td>
          </tbody>
        </table>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal"><%= __('Close')%></button>
        <button type="submit" class="btn btn-primary" id="btnSubcategoryConfirm">
          <%= __('Confirm')%>
        </button>
      </div>
    </div>
  </div>
  <span id="itemID" hidden></span>
</div>

<!-- modal confirmation row reorder -->
<div class="modal fade" tabindex="-1" role="dialog" id="subcategoryRowReorderModal">
  <div class="modal-dialog modal-lg" style="min-width: unset; text-align: initial;">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Are you sure you want to reorder?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="subcategoryRowReorderConfirmationBody">
        <p>Modal body text goes here.</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" onclick="saveSubcategoryReorder(1)">Save changes</button>
        <button type="button" class="btn btn-secondary" onclick="saveSubcategoryReorder(0)" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>