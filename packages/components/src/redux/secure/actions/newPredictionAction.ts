// import { createAction } from '../helpers'

// export function smartPredictionRequest({ cycle_lengths, period_lengths, age }) {
//   return createAction('SMART_PREDICTION_REQUEST', { cycle_lengths, period_lengths, age })
// }

// export function smartPredictionSuccess({ predicted_cycles, predicted_periods }) {
//   return createAction('SMART_PREDICTION_SUCCESS', {
//     predicted_cycles,
//     predicted_periods,
//   })
// }

// export function smartPredictionFailure({ error }) {
//   return createAction('SMART_PREDICTION_FAILURE', { error })
// }

// export function smartPredictionUpdate({ predicted_cycles, predicted_periods }) {
//   return createAction('UPDATE_SMART_PREDICTION', { predicted_cycles, predicted_periods })
// }
